# MCP SDK - pinned to v1 deliberately. SDK 2.0.0 renames FastMCP to MCPServer,
# so an unpinned install breaks the import in server.py.
mcp[cli]>=1.29,<2

python-dotenv
requests
uvicorn
anyio

# Only needed for the Google Ads connector. It is a heavy install - drop this
# line if you are not syncing Google Ads.
google-ads

# Only needed to run make_cert.py (self-signed TLS for --http team sharing,
# see SHARING.md). Not required for stdio use or plain-HTTP setups.
cryptography

# Only needed for ga4_sync.py (Google Analytics 4).
google-analytics-data

# Only needed for ga4_sync.py and merchant_center_sync.py — both use a Google
# service-account credential (server-to-server auth, no browser consent step).
google-auth

# Only needed for tiktok_creators_sync.py's `import` subcommand (loading a
# manually-exported .xlsx creator roster). The `api` subcommand and every
# other script here work without it.
openpyxl

# Required by algolia_sync.py and amazon_search_terms_monthly.py (YAML
# config files). Optional for amazon_ba_sync.py's brand_watchlist.yaml
# feature specifically — that one import is wrapped in a try/except, so
# amazon_ba_sync.py still runs its core reports without this installed, it
# just skips that one feature.
PyYAML

# Needed by klaviyo_sync.py and tiktok_live_sync.py, which use the stdlib
# `zoneinfo` module to convert timestamps into a shop's local calendar day.
# Windows does not ship the IANA time zone database the way Linux/macOS
# usually do, so `zoneinfo` raises ZoneInfoNotFoundError (even for "UTC")
# without this — install it everywhere for portability, it's a no-op where
# the OS already provides the data.
tzdata
